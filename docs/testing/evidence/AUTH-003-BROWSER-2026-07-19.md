# AUTH-003 Browser Evidence - 2026-07-19

## Scope

- Story: `AUTH-003 - Expired Session Redirect and Draft Recovery`
- Browser cases: `AUTH-003-01`, `02`, `03`, `04`, `05`, `06`, and `08`
- Runtime: Next.js 16.2.10 at `http://127.0.0.1:3103`
- Browser: Microsoft Edge through Playwright 1.61.1
- Workers / retries: 1 / 0
- Trace / screenshot: retained for every test

The fixture intercepts only `/api/**` calls. Navigation, React rendering, the real login form, `apiFetch`, the application shell, sign-out, browser history, and `beforeunload` execute in the real frontend.

## Independent Review Findings

The first nominally green run was rejected after trace review:

- AUTH-003-02 reset its notification maximum on the login document, so zero was not durable proof.
- AUTH-003-08 asserted only the eventual login URL while protected Synthetic content was briefly rendered after Back.

DEF-0027 and DEF-0028 record those findings. The shell and tests were hardened before the runs below.

## Focused Hardened Run

Cases 02 and 08 were executed alone first.

```text
AUTH-003-02 PASS - persistent maximum notification count remained zero
AUTH-003-08 PASS - cross-navigation protected-content exposure marker remained zero
2 passed (59.2s)
```

Artifact: `docs/testing/evidence/artifacts/AUTH-003-02-08-HARDENED-2026-07-19.txt`

## Final Full Run

```powershell
$env:AUTH003_BASE_URL='http://127.0.0.1:3103'
npx.cmd playwright test --config=playwright.auth003.config.ts
```

Result: **PASS - 7 passed, 0 failed, 0 skipped, 0 flaky.**

```json
{"startTime":"2026-07-19T16:21:26.278Z","duration":117993.421,"expected":7,"unexpected":0,"skipped":0,"flaky":0}
```

| Case | Result | Hardened browser proof |
|---|---|---|
| AUTH-003-01 | PASS | One expired protected action produced one login document request and preserved the complete local path/query. |
| AUTH-003-02 | PASS | Five distinct protected requests returned 401 together; one login document request occurred; a session-storage maximum that survives login navigation remained zero. |
| AUTH-003-03 | PASS | The real sign-in form returned to the exact same-origin route and query. |
| AUTH-003-04 | PASS | Absolute, protocol-relative, encoded external, backslash, and script-like return targets landed on the application default route. |
| AUTH-003-05 | PASS | Canceling two consecutive expiry navigations preserved the dirty draft and proved redirect-latch recovery. |
| AUTH-003-06 | PASS | Login API 401 displayed one actionable error without recursive navigation or duplicate errors. |
| AUTH-003-08 | PASS | The shell withheld protected children until authentication; a persistent observer recorded no transient protected heading after logout plus Back. |

Artifact: `docs/testing/evidence/artifacts/AUTH-003-BROWSER-HARDENED-FULL-2026-07-19.txt`.

Machine-readable reports and media:

- `frontend/test-results/auth-003/results.json`
- `frontend/test-results/auth-003/junit.xml`
- `frontend/test-results/auth-003/artifacts/`

## Type and Backend Regression Gates

- `npm.cmd run typecheck`: PASS, explicit `TYPECHECK_EXIT_CODE: 0` retained in `AUTH-003-TYPECHECK-2026-07-19.txt`.
- Full Maven regression: 284 tests, 0 failures, 0 errors, 1 explicitly skipped scenario-pack fixture; `MAVEN_TEST_EXIT_CODE: 0` retained in `FULL-MAVEN-REGRESSION-2026-07-19.txt`.

## Independent Review

PASS. The reviewer inspected the hardened traces and confirmed that AUTH-003-02 preserves a maximum notification count of zero across navigation and AUTH-003-08 shows only the neutral `Verifying session` state after Back, with an exposure marker of zero. All eight story cases are accepted.
